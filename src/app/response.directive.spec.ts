import { ResponseDirective } from './response.directive';

describe('ResponseDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponseDirective();
    expect(directive).toBeTruthy();
  });
});
